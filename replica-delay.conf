# Applied to postgres-replica2 only. On a standby this delays APPLYING WAL by
# the given interval, so the replica reliably lags behind the primary and stale
# reads are easy to observe. It does not delay RECEIVING WAL, so the replica can
# still acknowledge synchronous commits promptly.
recovery_min_apply_delay = '4s'